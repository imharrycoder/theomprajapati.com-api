# Local or production database provider
DATABASE_PROVIDER=postgresql

# Production database URL (PostgreSQL recommended for Render)
DATABASE_URL="postgresql://username:password@host:5432/database"

# Admin credentials for the API
ADMIN_USER=admin
ADMIN_PASS=your_secret_password
